function makeLine(size) {
    let line = '';
for (let i=0; i < size; i++) {
    line += '#';
}
return line;
}
console.log(makeLine(5));
//not sure why I put a 5 here though? Everything else I think I understand.